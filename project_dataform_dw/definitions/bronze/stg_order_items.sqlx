config {
    type: "view",
    schema: "bronze"
}

SELECT
    id AS order_item_id,
    order_id,
    user_id,
    product_id,
    sale_price,
    status,
    created_at
FROM ${constants.PUBLIC_DATA_PROJECT}.thelook_ecommerce.order_items